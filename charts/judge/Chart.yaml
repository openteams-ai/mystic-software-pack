apiVersion: v2
name: judge
description: >
  Nebari software pack for the vLLM judge (Qwen2.5-VL-7B-Instruct, served as
  model name "judge", OpenAI-compatible endpoint on port 8000).
  Cluster-internal only — no NebariApp (contract §7).
  Namespace: nebari-judge-pack.
type: application
version: 0.1.0
appVersion: "v0.8.5"
