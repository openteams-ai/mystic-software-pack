judge (vLLM) has been deployed (release: {{ .Release.Name }}).

Model: {{ include "judge.modelName" . }}
Served as: {{ .Values.model.servedModelName }}

Cluster-internal endpoint (contract §7):
  http://{{ .Values.service.name }}.{{ .Release.Namespace }}.svc:{{ .Values.service.port }}/v1

The judge is NOT exposed externally (no NebariApp). It is called only by the
checkmaite plugin from within the cluster.

GPU node: nodegroup=gpu (taint: nvidia.com/gpu=true:NoSchedule)
Model cache PVC: judge-model-cache ({{ .Values.cache.size }}, {{ .Values.cache.storageClassName }})
  Note: first-start downloads ~16 GB — readiness probe allows up to 20 min (failureThreshold=80).

Verify after first-start:
  kubectl run judgecheck -n {{ .Release.Namespace }} --rm -i --restart=Never \
    --image=curlimages/curl:8.11.0 -- \
    -s http://{{ .Values.service.name }}.{{ .Release.Namespace }}.svc:{{ .Values.service.port }}/v1/models

Fallback model ({{ .Values.model.fallback }}):
  If the 7B OOMs, set --set model.useFallback=true and redeploy.
